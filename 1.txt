/* Center the entire search-container */
.search-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  margin: 20px auto;
  width: 50%; /* Adjust the width for better alignment */
}

/* Style the search bar */
.search-bar {
  width: 100%;
  padding: 10px;
  font-size: 16px;
  border: 1px solid #ccc;
  border-radius: 4px;
  margin-bottom: 10px;
  text-align: center;
}

/* Style the search button */
.search-button {
  padding: 10px 20px;
  background-color: #007bff;
  color: #fff;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.3s ease;
}

.search-button:hover {
  background-color: #0056b3;
}

/* Style the suggestions dropdown */
.suggestions {
  list-style: none;
  padding: 0;
  margin: 0;
  width: 100%;
  border: 1px solid #ccc;
  border-radius: 4px;
  max-height: 150px;
  overflow-y: auto;
  background-color: #fff;
}

.suggestions li {
  padding: 10px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.suggestions li:hover {
  background-color: #f0f0f0;
}

/* Center the response container */
.response-container {
  margin: 20px auto;
  text-align: center; /* Align text and content inside the container */
  width: 50%; /* Adjust the width for alignment */
  background-color: #f9f9f9;
  border: 1px solid #ddd;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0px 2px 6px rgba(0, 0, 0, 0.1);
}

/* Styling for the title */
h1 {
  text-align: center;
  color: #333;
  margin-bottom: 20px;
}
