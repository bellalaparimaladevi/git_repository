<div class="buttons">
  <button (click)="fetchUsersFirstThenReposConcatMap()">Fetch Users then Repos with concatMap</button>
  <button (click)="fetchReposFirstThenUsersConcatMap()">Fetch Repos then Users with concatMap</button>
  <button (click)="fetchUsersForkJoin()">Fork join failure</button>
  <button (click)="fetchUsersReposForkJoin()">Fetch users and repos with forkJoin</button>
</div>

<div class="response">
  @for (response of responses; track $index) {
    <div>
      <table>
        <thead>
          <tr>
            <th>Type</th>
            <th>Data</th>
          </tr>
        </thead>
        <tbody>
          @for (item of response.data; track $index) {
            <tr>
              <td>{{ item.type }}</td>
              <td>{{ item.data | json }}</td>
            </tr>
          }
        </tbody>
      </table>
    </div>
  }
</div>
